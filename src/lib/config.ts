import { dev } from "$app/environment";

export const title = "Markdown Blog";
export const description = "Sveltekit blog for exercise"
export const url = dev ? 'http://localhost:5173' : 'vercel url'